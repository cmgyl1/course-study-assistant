# =====================================================================
# 大学生课程学习辅助软件 —— Python 依赖
#
# 参考环境：Python 3.12.10（Windows）
# 安装：  pip install -r requirements.txt
#
# 说明：
#   - 本项目不依赖任何在线大模型服务，全部能力在本地离线运行。
#   - 检索使用自实现的 **BM25 词法检索**（含标题索引 + 查询放宽 + 拒答），
#     零额外依赖、无需下载任何语义模型。
#     历史上曾用 chromadb + 自写 n-gram 哈希向量，实测排序信息丢失
#     （score 恒为 0），已整体移除。
#   - 打包为 exe 需要 PyInstaller；日常开发可不装。
# =====================================================================

# ---- GUI ----
PySide6==6.11.2

# ---- PDF 解析 / 版面还原 / 图像裁切 ----
PyMuPDF==1.28.2

# ---- OCR（扫描版教材取字；onnxruntime 为 CPU 推理后端）----
rapidocr-onnxruntime==1.4.4
onnxruntime==1.20.1

# ---- 中文分词（知识点提取 / 检索分词）----
jieba==0.42.1

# ---- 数值计算 ----
numpy==2.5.2

# ---- 题库与资料格式转换 ----
python-docx==1.2.0
python-pptx==1.0.2
pypandoc==1.17

# ---- 网络请求（教材资料抓取脚本用）----
requests==2.34.2

# ---- 打包（可选）----
pyinstaller==6.22.2
